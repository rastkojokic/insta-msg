<input type="hidden" id="dialect" value="<%= session[:check_in]["dialect"] %>"/>
<input type="hidden" id="username" value="<%= session[:check_in]["username"] %>"/>

<div class="half-div">
  <label for="usernameSpan">Hi </label>
  <span id="usernameSpan">
    <%= session[:check_in]["username"] %>,
  </span>

  <br>
  <label for="dialectSpan">dialect:</label>
  <span id="dialectSpan">
    <%= dialect_display_name(session[:check_in]["dialect"]) %>
  </span>

  <br>
  <textarea id="newMessageTextArea"></textarea>

  <br>
  <button id="sendBtn">Send</button>

  <div id="messageList">
  </div>
</div>

<div class="half-div">
  <h2>Online users</h2>
  <ul id="onlineUsersList">
  </ul>
</div>
