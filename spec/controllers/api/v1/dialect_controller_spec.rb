require "spec_helper"

describe Api::V1::DialectController do
  describe "GET translate" do
    context "valid request" do
      it "calls translation's translate method with dialect and message text" do
      end
    end

    context "bad request" do
      it "responds with 400 status and message saying it was a bad request" do
      end
    end
  end
end
